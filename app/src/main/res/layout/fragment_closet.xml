<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FBF8FE">

    <!-- Sticky Tab Layout header -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FBF8FE"
        app:elevation="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="20dp"
            android:paddingTop="20dp"
            android:paddingBottom="8dp">

            <!-- Header with Title and Edit Button on same line -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Left side: Title -->
                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="My Closet"
                    android:textSize="26sp"
                    android:textStyle="bold"
                    android:textColor="#1A1A1A" />

                <!-- Right side: Edit Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnEditCloset"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Edit"
                    android:textColor="#5D5CDE"
                    app:cornerRadius="8dp"
                    android:backgroundTint="@android:color/transparent"
                    app:strokeColor="#5D5CDE"
                    app:strokeWidth="1dp"
                    android:textAllCaps="false"
                    android:paddingHorizontal="12dp" />

            </LinearLayout>

            <!-- Item Count -->
            <TextView
                android:id="@+id/tvItemCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0 items"
                android:textSize="14sp"
                android:textColor="#888888"
                android:layout_marginTop="2dp" />

        </LinearLayout>

        <!-- Tab Layout -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#FBF8FE"
            app:tabMode="scrollable"
            app:tabGravity="start"
            app:tabSelectedTextColor="#5D5CDE"
            app:tabTextColor="#888888"
            app:tabIndicatorColor="#5D5CDE"
            app:tabIndicatorHeight="2dp"
            app:tabPaddingStart="12dp"
            app:tabPaddingEnd="12dp">

            <!-- Tabs added programmatically in ClosetFragment.kt -->
        </com.google.android.material.tabs.TabLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Closet grid -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCloset"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="80dp"
        android:clipToPadding="false"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- Empty state (shown when closet is empty) -->
    <LinearLayout
        android:id="@+id/emptyState"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_checkroom"
            app:tint="#DCD9DE" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your closet is empty"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#888888"
            android:layout_marginTop="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap + to upload your first item"
            android:textSize="14sp"
            android:textColor="#AAAAAA"
            android:layout_marginTop="4dp" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>